"""
UltraRF LDPC Codec (skeleton)
Implements encode/decode interface for LDPC FEC.
"""
import numpy as np

class LDPCCodec:
    def encode(self, bits):
        # Placeholder: return bits unchanged
        return np.array(bits)

    def decode(self, codeword):
        # Placeholder: return codeword unchanged
        return np.array(codeword)
